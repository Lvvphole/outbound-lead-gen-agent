# \# Outbound Lead Gen SkillOps Runtime

# 

# Python FastAPI, Typer, Postgres/SQLAlchemy/Alembic, Docker Compose local-first runtime. Vercel is frontend-later only. Supabase is optional managed Postgres only via DATABASE\_URL. Twilio, HubSpot, and Calendly adapters are mocked by default. First-party consent only. LLM may talk; SkillOps decides; Metrics judge; Evidence proves; Verifier gates; Humans handle exceptions.

# 

# \## Setup

# `pip install -e .\[dev]` then `python -m skillops run-demo`.

# 

# \## Docker

# `docker compose config` and `docker compose build`.

# 

# \## Compliance disclaimer

# Not legal advice; state rules are LEGAL\_REVIEW\_REQUIRED until reviewed.

# 

# \## Terminal states

# See `skillops/runtime/schemas.py`.

# 

# \## Rollback

# Revert candidate versions and use changelog rollback notes.

